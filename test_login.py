import pytest

from login import login

def test_login():
    pass
    # inputs = iter([13.15, 'Meilen', 'Seemeilen'])
    # monkeypatch.setattr('builtins.input', lambda _: next(inputs))
    # lengthcalc.main()
    # captured = capsys.readouterr()
    # lines = captured.out.splitlines()
    # assert lines[len(lines) - 1] == '13.15 Meilen = 11.427045275688759 Seemeilen'
